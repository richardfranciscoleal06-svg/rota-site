# Required for signed sessions and secure cookie validation in Netlify Functions.
SESSION_SECRET=
# Example: openssl rand -hex 32

# Required only for the initial bootstrap of the first admin account.
BOOTSTRAP_TOKEN=
# Example: openssl rand -hex 32

# Recommended for local Netlify dev and deploy previews.
NODE_ENV=development
